version: '3.8'

services:
  # Servicio de base de datos PostgreSQL
  postgres:
    image: postgres:15
    container_name: trazanet-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-truc0}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-retrucovale4}
      POSTGRES_DB: ${POSTGRES_DB:-trazaNet}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    networks:
      - trazanet-network

  # Servicio pgAdmin para administrar la base de datos
  pgadmin:
    image: dpage/pgadmin4
    container_name: trazanet-pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@trazanet.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin123}
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - trazanet-network

  # Servicio backend (Python/FastAPI)
  backend:
    build:
      context: ../backend
      dockerfile: ../docker/Dockerfile.backend
    container_name: trazanet-backend
    restart: always
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=postgresql+psycopg://truc0:retrucovale4@postgres/trazaNet
      - DEVELOPMENT_PROFILE=production
      - MODEL_NAME=gpt-3.5-turbo
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ../backend:/app
    depends_on:
      - postgres
    networks:
      - trazanet-network

  # Servicio frontend (Angular)
  frontend:
    build:
      context: ../frontend
      dockerfile: ../docker/Dockerfile.frontend
    container_name: trazanet-frontend
    restart: always
    ports:
      - "4200:80"
    volumes:
      - ../frontend:/app
    depends_on:
      - backend
    networks:
      - trazanet-network

networks:
  trazanet-network:
    driver: bridge

volumes:
  postgres_data: 